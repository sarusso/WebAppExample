
# Policy for DockerOps .gitignore: ignore ONLY hidden files or very standard one (i.e. pyc, pip-log.txt)
# No folders like 'dist', 'build', 'download' are ignored due to the obvious risk of huge troubles.

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
.eggs/
*.egg-info/
.installed.cfg
*.egg

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
.tox/
.coverage
.coverage.*
.cache
*,cover

# OSX
.DS_Store

# Eclipse
.project
.pydevproject
.settings

#----------------
# Cloud service
#----------------

# Devel
devel/*.sqlite3
devel/eDjango
devel/venv

# Webapp service migrations
services/web/code/migrations/*.py
services/web/code/migrations/*.pyc
!services/web/code/migrations/__init__.py


# Keys and sensible (local) conf
services/proxy/certificates/*
services/web/db_conf.sh

#---------------
# Reyns
#---------------
.Reyns
data_*
host.conf
reyns/*
!reyns/setup

# Daemon
.update_in_progress_flag
