
This is an empty folder which is used as mount point for a temporary
volume to allow using "ephemeral" migrations when running in development
mode (with the code mounted directly in the container) without any symbolic
links which are not correctly handled on some file systems (i.e. Windows).

